--- a/cmake/LLVM.cmake	2016-11-16 10:15:27.000000000 -0600
+++ b/cmake/LLVM.cmake	2017-02-20 16:49:04.965353500 -0600
@@ -223,7 +223,7 @@ endforeach()
 ####################################################################
 
 macro(find_program_or_die OUTPUT_VAR PROG_NAME DOCSTRING)
-  find_program(${OUTPUT_VAR} NAMES "${PROG_NAME}${LLVM_BINARY_SUFFIX}${CMAKE_EXECUTABLE_SUFFIX}" "${PROG_NAME}${CMAKE_EXECUTABLE_SUFFIX}" HINTS "${LLVM_BINDIR}" "${LLVM_CONFIG_LOCATION}" "${LLVM_PREFIX}" "${LLVM_PREFIX_BIN}" DOC "${DOCSTRING}")
+  find_program(${OUTPUT_VAR} NAMES "${PROG_NAME}${LLVM_BINARY_SUFFIX}" "${PROG_NAME}" HINTS "${LLVM_BINDIR}" "${LLVM_CONFIG_LOCATION}" "${LLVM_PREFIX}" "${LLVM_PREFIX_BIN}" DOC "${DOCSTRING}")
   if(${OUTPUT_VAR})
     message(STATUS "Found ${PROG_NAME}: ${${OUTPUT_VAR}}")
   else()
--- a/lib/CL/CMakeLists.txt	2016-11-16 10:15:27.000000000 -0600
+++ b/lib/CL/CMakeLists.txt	2017-02-20 19:02:44.337303200 -0600
@@ -198,4 +198,5 @@ set_target_properties("${POCL_LIBRARY_NA
 target_link_libraries("${POCL_LIBRARY_NAME}" PUBLIC ${POCL_PUBLIC_LINK_LIST} PRIVATE ${POCL_PRIVATE_LINK_LIST} )
 install(TARGETS "${POCL_LIBRARY_NAME}"
         LIBRARY DESTINATION ${POCL_INSTALL_PUBLIC_LIBDIR}
-        ARCHIVE DESTINATION ${POCL_INSTALL_PUBLIC_LIBDIR}/static)
+        RUNTIME DESTINATION ${POCL_INSTALL_PUBLIC_BINDIR}
+        ARCHIVE DESTINATION ${POCL_INSTALL_PUBLIC_LIBDIR})
--- a/lib/poclu/CMakeLists.txt	2016-11-16 10:15:27.000000000 -0600
+++ b/lib/poclu/CMakeLists.txt	2017-02-20 19:02:57.002425900 -0600
@@ -36,5 +36,6 @@ target_link_libraries("poclu" ${LIBMATH}
 
 install(TARGETS "poclu"
         LIBRARY DESTINATION ${POCL_INSTALL_PUBLIC_LIBDIR}
-        ARCHIVE DESTINATION ${POCL_INSTALL_PUBLIC_LIBDIR}/static)
+        RUNTIME DESTINATION ${POCL_INSTALL_PUBLIC_BINDIR}
+        ARCHIVE DESTINATION ${POCL_INSTALL_PUBLIC_LIBDIR})
 
